var input = document.getElementById("response");    // get input element from html
input.onclick = checkAnswer();                      // call checkAnswer when input is submitted

function checkAnswer(answer) {
    
}